node {
    version = '8.0.0'
    npmVersion = '5.0.0'
    download = true
    workDir = file("${project.buildDir}/nodejs")
}

npmInstall {
    args = ['--no-bin-links']
}

processResources.dependsOn(npmInstall)
processResources.dependsOn(npm_run_build)
npm_run_build.mustRunAfter npmInstall
